import { LocationValidator } from './location-validator.directive';

describe('LocationValidator', () => {
  it('should create an instance', () => {
    const directive = new LocationValidator();
    expect(directive).toBeTruthy();
  });
});
